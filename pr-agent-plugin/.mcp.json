{
  "mcpServers": {
    "pr-agent": {
      "command": "node",
      "args": ["${CLAUDE_PLUGIN_ROOT}/server/index.js"],
      "env": {
        "PR_AGENT_ROOT": "${CLAUDE_PLUGIN_ROOT}/.."
      }
    }
  }
}
